This package demonstrates issues with tsconfig extends using lerna.

To test:

* `npm install -g lerna`
* `lerna bootstrap`
* `lerna run build`

Notice that the 2nd package does not actually complain about the syntactical errors.